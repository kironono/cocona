machine:
  timezone: Asia/Tokyo
  ruby:
    version: 2.3.1
  services:
    - redis

dependencies:
  pre:
    - sudo apt-get install -y xvfb libqt4-dev libqtwebkit-dev
  post:
    - >
      mkdir -p config;
      cp config/settings.yml.sample config/settings.yml;
