machine:
  timezone: Asia/Tokyo
  ruby:
    version: 2.3.1
  services:
    - redis

dependencies:
  post:
    - >
      mkdir -p config;
      echo -e 'host: localhost\nport: 6379' > config/redis.yml;
      cp config/settings.yml.sample config/settings.yml;
